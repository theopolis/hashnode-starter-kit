fragment Post on Post {
	id
	title
	subtitle
	url
	author {
		name
		profilePicture
	}
	publishedAt
	slug
	brief
	series {
		id
		name
		slug
	}
	tags {
		id
		name
		slug
	}
	comments(first: 0) {
		totalDocuments
	}
}
