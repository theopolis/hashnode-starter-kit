query PostsBySeries($host: String!, $seriesSlug: String!, $first: Int!, $after: String) {
	publication(host: $host) {
		...Publication
		series(slug: $seriesSlug) {
			id
			name
			slug
			description {
				text
			}
			coverImage
			posts(first: $first, after: $after) {
				edges {
					node {
						...Post
					}
				}
				pageInfo {
					...PageInfo
				}
			}
		}
	}
}
